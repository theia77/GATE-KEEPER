EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
# Base URL of the Next.js web app's API routes (apps/web) — mobile calls the same
# /api/* handlers as web, authenticated via Bearer token instead of cookies.
EXPO_PUBLIC_API_URL=https://your-web-app.example.com
