// Placeholder landing page — full sidebar layout and feature pages land in Phase 4.
export default function HomePage() {
  return (
    <main style={{ minHeight: "100vh", background: "#0b0a09", color: "#f5f1ea", display: "flex", alignItems: "center", justifyContent: "center" }}>
      <p>GATE FORCE — web app scaffold (Phase 3 API layer complete, Phase 4 UI next).</p>
    </main>
  );
}
