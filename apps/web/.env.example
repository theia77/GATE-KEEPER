NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Server-only. Never exposed to the browser — required by route handlers that must
# bypass RLS deliberately (signed URL issuance, custom mock upload parsing).
SUPABASE_SERVICE_ROLE_KEY=
